// create a projects section
// I need a height of 500px, it also need a id, for the a tag to scroll to it

const Projects = () => {
  return (
    <div id="projects" className="min-h-screen">
      ProjectsSection
    </div>
  );
};

export default Projects;
